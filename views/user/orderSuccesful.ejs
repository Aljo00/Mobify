<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Order Placed Successfully - Evara</title>
    <!-- <link rel="stylesheet" href="assets/css/styles.css" />
    <link rel="stylesheet" href="assets/css/orderplace.css" /> -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
    />
    <style>
      .page-container {
        min-height: 100vh;
        background-color: #f8f9fa;
        padding: 20px;
        opacity: 0;
        animation: fadeInPage 0.6s ease-out forwards;
      }

      .order-success-container {
        max-width: 1000px;
        margin: 20px auto;
        padding: 30px;
        background-color: #fff;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        border-radius: 15px;
      }

      .order-content {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 30px;
        margin-top: 30px;
        opacity: 0;
        animation: slideUpFade 0.8s ease-out 0.3s forwards;
      }

      .success-header {
        text-align: center;
        margin-bottom: 30px;
        grid-column: 1 / -1;
        opacity: 0;
        animation: popIn 0.6s cubic-bezier(0.16, 1, 0.3, 1) 0.6s forwards;
      }

      .success-icon {
        width: 60px;
        height: 60px;
        background-color: #28a745;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 15px;
        animation: bounceIn 0.8s cubic-bezier(0.68, -0.55, 0.265, 1.55) 0.9s forwards;
        opacity: 0;
        transform: scale(0);
      }

      .success-icon i {
        color: white;
        font-size: 30px;
      }

      .order-meta {
        background-color: #f8f9fa;
        padding: 20px;
        border-radius: 10px;
        height: fit-content;
        opacity: 0;
        transform: translateX(-20px);
        animation: slideInLeft 0.6s ease-out 1s forwards;
      }

      .address-details {
        background-color: #fff;
        padding: 15px;
        border-radius: 8px;
        border: 1px solid #e0e0e0;
        margin-top: 10px;
      }

      .ordered-items {
        background-color: #f8f9fa;
        padding: 20px;
        border-radius: 10px;
        max-height: 60vh;
        overflow-y: auto;
        opacity: 0;
        transform: translateX(20px);
        animation: slideInRight 0.6s ease-out 1s forwards;
      }

      .item-card {
        background-color: #fff;
        border: 1px solid #e0e0e0;
        border-radius: 8px;
        padding: 15px;
        margin-bottom: 10px;
        transition: transform 0.2s;
        opacity: 0;
        animation: fadeInUp 0.5s ease-out forwards;
        animation-delay: calc(1.2s + (var(--item-index, 0) * 0.1s));
      }

      .item-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(0,0,0,0.1);
      }

      .total-amount {
        background-color: #28a745;
        color: white;
        padding: 10px 20px;
        border-radius: 8px;
        margin-top: 20px;
      }

      .delivery-time {
        background-color: #e8f5e9;
        color: #2e7d32;
        padding: 15px;
        border-radius: 8px;
        margin: 20px 0;
        text-align: center;
        grid-column: 1 / -1;
        opacity: 0;
        animation: fadeInUp 0.5s ease-out 1.8s forwards;
      }

      .btn-view-orders {
        grid-column: 1 / -1;
        width: fit-content;
        margin: 0 auto;
        padding: 10px 30px;
        opacity: 0;
        animation: fadeInUp 0.5s ease-out 2s forwards;
      }

      @media (max-width: 768px) {
        .order-content {
          grid-template-columns: 1fr;
        }
        
        .ordered-items {
          max-height: 40vh;
        }
      }

      /* New Animation Keyframes */
      @keyframes fadeInPage {
        from { opacity: 0; }
        to { opacity: 1; }
      }

      @keyframes slideUpFade {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes popIn {
        from {
          opacity: 0;
          transform: scale(0.8);
        }
        to {
          opacity: 1;
          transform: scale(1);
        }
      }

      @keyframes bounceIn {
        0% {
          opacity: 0;
          transform: scale(0);
        }
        50% {
          transform: scale(1.2);
        }
        70% {
          transform: scale(0.9);
        }
        100% {
          opacity: 1;
          transform: scale(1);
        }
      }

      @keyframes slideInLeft {
        from {
          opacity: 0;
          transform: translateX(-20px);
        }
        to {
          opacity: 1;
          transform: translateX(0);
        }
      }

      @keyframes slideInRight {
        from {
          opacity: 0;
          transform: translateX(20px);
        }
        to {
          opacity: 1;
          transform: translateX(0);
        }
      }

      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(10px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      /* Add smooth transitions */
      .item-card {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
      }

      /* Loading animation */
      .loading-overlay {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: #fff;
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 9999;
        transition: opacity 0.5s ease-out;
      }

      .loading-spinner {
        width: 50px;
        height: 50px;
        border: 3px solid #f3f3f3;
        border-top: 3px solid #28a745;
        border-radius: 50%;
        animation: spin 1s linear infinite;
      }

      @keyframes spin {
        0% { transform: rotate(0deg); }
        100% { transform: rotate(360deg); }
      }
    </style>
  </head>
  <body>

    <div class="page-container">
      <div class="order-success-container">
        <div class="order-content">
          <% if (!order) { %>
            <div class="error-message">
              <h2>Order Not Found</h2>
              <p>Sorry, we couldn't find the order you're looking for.</p>
              <a href="/orders" class="btn btn-primary">View All Orders</a>
            </div>
          <% } else { %>
            <div class="success-header">
              <div class="success-icon">
                <i class="fas fa-check"></i>
              </div>
              <h2>
                <% if (order.status === "Payment Pending") { %>
                  Order Created - Payment Pending
                <% } else { %>
                  Order Placed Successfully!
                <% } %>
              </h2>
              <p class="text-muted">
                <% if (order.status === "Payment Pending") { %>
                  Please complete your payment to confirm the order.
                <% } else { %>
                  Thank you for shopping with us.
                <% } %>
              </p>
            </div>

            <div class="order-meta">
              <h4 class="mb-4">Order Information</h4>
              <div class="mb-3">
                <strong>Order ID:</strong>
                <div class="text-muted"><%= order.orderId || 'N/A' %></div>
              </div>
              <div class="mb-3">
                <strong>Order Date:</strong>
                <div class="text-muted"><%= order.createdAt ? order.createdAt.toDateString() : 'N/A' %></div>
              </div>
              <div class="mb-4">
                <strong>Delivery Address:</strong>
                <% if (order.formattedAddress) { %>
                  <div class="address-details">
                    <strong class="d-block mb-2"><%= order.formattedAddress.addressType %></strong>
                    <div class="text-muted">
                      <%= order.formattedAddress.houseName %><br>
                      <%= order.formattedAddress.city %>, 
                      <%= order.formattedAddress.state %> - 
                      <%= order.formattedAddress.pincode %><br>
                      📱 <%= order.formattedAddress.phone %><br>
                      <% if (order.formattedAddress.altPhone) { %>
                        📞 <%= order.formattedAddress.altPhone %>
                      <% } %>
                    </div>
                  </div>
                <% } else { %>
                  <div class="address-details">
                    <p class="text-muted">Address information not available</p>
                  </div>
                <% } %>
              </div>
            </div>

            <div class="ordered-items">
              <h4 class="mb-4">Order Summary</h4>
              <% if (order.formattedItems && order.formattedItems.length > 0) { %>
                <% order.formattedItems.forEach((item, index) => { %>
                  <div class="item-card" style="--item-index: <%= index %>">
                    <h6 class="mb-2"><%= item.name %></h6>
                    <div class="text-muted small mb-2">
                      <%= item.specifications.RAM %> RAM | 
                      <%= item.specifications.Storage %> Storage | 
                      <%= item.specifications.color %>
                    </div>
                    <div class="d-flex justify-content-between align-items-center">
                      <span class="text-muted">Qty: <%= item.quantity %></span>
                      <span class="fw-bold">₹<%= item.totalPrice.toFixed(2) %></span>
                    </div>
                  </div>
                <% }) %>
                <div class="total-amount">
                  <div class="d-flex justify-content-between align-items-center">
                    <span>Total Amount</span>
                    <span class="h5 mb-0">₹<%= order.totalPrice.toFixed(2) %></span>
                  </div>
                </div>
              <% } else { %>
                <p class="text-muted">No items found in this order</p>
              <% } %>
            </div>

            <div class="delivery-time">
              <i class="fas fa-truck me-2"></i>
              Expected Delivery: Within 3-5 Business Days
            </div>

            <a href="/orders" class="btn btn-primary btn-lg btn-view-orders">
              View My Orders
            </a>
          <% } %>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
