<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Order Placed Successfully - Evara</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
    />
    <style>
      :root {
        --primary: #4f46e5;
        --success: #059669;
        --warning: #eab308;
        --danger: #dc2626;
        --background: #f8fafc;
        --surface: #ffffff;
        --text: #1e293b;
        --text-light: #64748b;
        --border: #e2e8f0;
      }

      body {
        margin: 0;
        min-height: 100vh;
        font-family: 'Inter', system-ui, -apple-system, sans-serif;
        color: var(--text);
      }

      body.payment-pending {
        background: linear-gradient(135deg, #7928CA 0%, #FF0080 100%);
      }

      body.pending {
        background: linear-gradient(135deg, #3B82F6 0%, #8B5CF6 100%);
      }

      body.success {
        background: linear-gradient(135deg, #0EA5E9 0%, #10B981 100%);
      }

      body.cancelled {
        background: linear-gradient(135deg, #EF4444 0%, #F43F5E 100%);
      }

      @keyframes slideIn {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }

      @keyframes scaleIn {
        from {
          transform: scale(0.9);
          opacity: 0;
        }
        to {
          transform: scale(1);
          opacity: 1;
        }
      }

      .page-container {
        position: relative;
        z-index: 1;
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem;
        animation: fadeIn 0.5s ease-out;
      }

      .order-success-container {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(20px);
        border-radius: 24px;
        box-shadow: 
          0 4px 6px -1px rgba(0, 0, 0, 0.1),
          0 10px 15px -3px rgba(0, 0, 0, 0.1),
          0 20px 25px -5px rgba(0, 0, 0, 0.1);
        margin-top: 2rem;
        overflow: hidden;
        border: 1px solid rgba(255, 255, 255, 0.2);
        animation: slideIn 0.8s ease-out;
      }

      .success-header {
        position: relative;
        text-align: center;
        padding: 3rem 2rem;
        overflow: hidden;
        animation: scaleIn 0.8s ease-out;
      }

      .success-header.payment-pending {
        background: linear-gradient(to right, rgba(121, 40, 202, 0.1), rgba(255, 0, 128, 0.05));
      }

      .success-header.pending {
        background: linear-gradient(to right, rgba(59, 130, 246, 0.1), rgba(139, 92, 246, 0.05));
      }

      .success-header.success {
        background: linear-gradient(to right, rgba(14, 165, 233, 0.1), rgba(16, 185, 129, 0.05));
      }

      .success-header.cancelled {
        background: linear-gradient(to right, rgba(239, 68, 68, 0.1), rgba(244, 63, 94, 0.05));
      }

      .success-icon {
        width: 80px;
        height: 80px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1.5rem;
        font-size: 32px;
        color: white;
        position: relative;
        background: conic-gradient(from 0deg, rgba(255,255,255,0.4), rgba(255,255,255,0.1));
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
      }

      .payment-pending .success-icon {
        background: linear-gradient(135deg, #7928CA, #FF0080);
      }

      .pending .success-icon {
        background: linear-gradient(135deg, #3B82F6, #8B5CF6);
      }

      .success .success-icon {
        background: linear-gradient(135deg, #0EA5E9, #10B981);
      }

      .cancelled .success-icon {
        background: linear-gradient(135deg, #EF4444, #F43F5E);
      }

      .order-content {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        padding: 2rem;
      }

      .order-meta {
        background: rgba(255, 255, 255, 0.5);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        padding: 2rem;
        border: 1px solid rgba(255, 255, 255, 0.2);
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
        transition: transform 0.2s ease;
        animation: slideIn 0.8s ease-out 0.2s both;
      }

      .ordered-items {
        background: rgba(255, 255, 255, 0.5);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        padding: 2rem;
        border: 1px solid rgba(255, 255, 255, 0.2);
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
        transition: transform 0.2s ease;
        animation: slideIn 0.8s ease-out 0.4s both;
      }

      .payment-pending .order-meta {
        background: linear-gradient(135deg, rgba(121, 40, 202, 0.1), rgba(255, 0, 128, 0.05));
      }

      .pending .order-meta {
        background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(139, 92, 246, 0.05));
      }

      .success .order-meta {
        background: linear-gradient(135deg, rgba(14, 165, 233, 0.1), rgba(16, 185, 129, 0.05));
      }

      .cancelled .order-meta {
        background: linear-gradient(135deg, rgba(239, 68, 68, 0.1), rgba(244, 63, 94, 0.05));
      }

      .payment-pending .ordered-items {
        background: linear-gradient(135deg, rgba(255, 0, 128, 0.1), rgba(121, 40, 202, 0.05));
      }

      .pending .ordered-items {
        background: linear-gradient(135deg, rgba(139, 92, 246, 0.1), rgba(59, 130, 246, 0.05));
      }

      .success .ordered-items {
        background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(14, 165, 233, 0.05));
      }

      .cancelled .ordered-items {
        background: linear-gradient(135deg, rgba(244, 63, 94, 0.1), rgba(239, 68, 68, 0.05));
      }

      .order-meta h4, .ordered-items h4 {
        font-size: 1.25rem;
        font-weight: 600;
        margin-bottom: 1.5rem;
        padding-bottom: 1rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.2);
        color: var(--text);
      }

      .item-card {
        display: grid;
        grid-template-columns: auto 1fr auto;
        gap: 1.5rem;
        align-items: center;
        padding: 1.25rem;
        background: rgba(255, 255, 255, 0.7);
        border-radius: 16px;
        margin-bottom: 1rem;
        transition: all 0.3s ease;
        border: 1px solid rgba(255, 255, 255, 0.2);
        animation: slideIn 0.5s ease-out;
        animation-fill-mode: both;
      }

      .item-card:nth-child(1) { animation-delay: 0.6s; }
      .item-card:nth-child(2) { animation-delay: 0.7s; }
      .item-card:nth-child(3) { animation-delay: 0.8s; }
      .item-card:nth-child(4) { animation-delay: 0.9s; }

      .item-card:hover {
        transform: translateY(-2px) scale(1.02);
        background: rgba(255, 255, 255, 0.9);
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
      }

      .item-image {
        width: 80px;
        height: 80px;
        border-radius: 12px;
        object-fit: cover;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
      }

      .item-details {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
      }

      .item-name {
        font-weight: 600;
        font-size: 1.1rem;
        color: var(--text);
      }

      .item-price {
        font-size: 1rem;
        font-weight: 500;
      }

      .payment-pending .item-price {
        color: #7928CA;
      }

      .pending .item-price {
        color: #3B82F6;
      }

      .success .item-price {
        color: #10B981;
      }

      .cancelled .item-price {
        color: #EF4444;
      }

      .status-badge {
        display: inline-flex;
        align-items: center;
        padding: 0.75rem 1.5rem;
        border-radius: 9999px;
        font-size: 0.875rem;
        font-weight: 600;
        margin-bottom: 1rem;
        text-transform: uppercase;
        letter-spacing: 0.05em;
      }

      .payment-pending .status-badge {
        background: linear-gradient(135deg, #7928CA20, #FF008020);
        color: #7928CA;
      }

      .pending .status-badge {
        background: linear-gradient(135deg, #3B82F620, #8B5CF620);
        color: #3B82F6;
      }

      .success .status-badge {
        background: linear-gradient(135deg, #0EA5E920, #10B98120);
        color: #10B981;
      }

      .cancelled .status-badge {
        background: linear-gradient(135deg, #EF444420, #F43F5E20);
        color: #EF4444;
      }

      .action-button {
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
        padding: 1rem 2rem;
        border-radius: 12px;
        font-weight: 600;
        font-size: 1rem;
        text-decoration: none;
        transition: all 0.2s ease;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        margin-top: 2rem;
      }

      .payment-pending .action-button {
        background: linear-gradient(135deg, #7928CA, #FF0080);
        color: white;
        box-shadow: 0 8px 16px rgba(121, 40, 202, 0.2);
      }

      .pending .action-button {
        background: linear-gradient(135deg, #3B82F6, #8B5CF6);
        color: white;
        box-shadow: 0 8px 16px rgba(59, 130, 246, 0.2);
      }

      .success .action-button {
        background: linear-gradient(135deg, #0EA5E9, #10B981);
        color: white;
        box-shadow: 0 8px 16px rgba(14, 165, 233, 0.2);
      }

      .cancelled .action-button {
        background: linear-gradient(135deg, #EF4444, #F43F5E);
        color: white;
        box-shadow: 0 8px 16px rgba(239, 68, 68, 0.2);
      }

      .action-button:hover {
        transform: translateY(-2px);
        box-shadow: 0 12px 20px rgba(0, 0, 0, 0.2);
      }

      .background-pattern {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        opacity: 0.4;
        pointer-events: none;
        z-index: 0;
      }

      .payment-pending .background-pattern {
        background-image: 
          linear-gradient(45deg, #7928CA10 25%, transparent 25%),
          linear-gradient(-45deg, #7928CA10 25%, transparent 25%),
          linear-gradient(45deg, transparent 75%, #7928CA10 75%),
          linear-gradient(-45deg, transparent 75%, #7928CA10 75%);
        background-size: 40px 40px;
      }

      .pending .background-pattern {
        background-image: 
          linear-gradient(45deg, #3B82F610 25%, transparent 25%),
          linear-gradient(-45deg, #3B82F610 25%, transparent 25%),
          linear-gradient(45deg, transparent 75%, #3B82F610 75%),
          linear-gradient(-45deg, transparent 75%, #3B82F610 75%);
        background-size: 40px 40px;
      }

      .success .background-pattern {
        background-image: 
          linear-gradient(45deg, #0EA5E910 25%, transparent 25%),
          linear-gradient(-45deg, #0EA5E910 25%, transparent 25%),
          linear-gradient(45deg, transparent 75%, #0EA5E910 75%),
          linear-gradient(-45deg, transparent 75%, #0EA5E910 75%);
        background-size: 40px 40px;
      }

      .cancelled .background-pattern {
        background-image: 
          linear-gradient(45deg, #EF444410 25%, transparent 25%),
          linear-gradient(-45deg, #EF444410 25%, transparent 25%),
          linear-gradient(45deg, transparent 75%, #EF444410 75%),
          linear-gradient(-45deg, transparent 75%, #EF444410 75%);
        background-size: 40px 40px;
      }

      @keyframes shine {
        to {
          transform: translateX(100%);
        }
      }

      .success-header::after {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.2), transparent);
        transform: translateX(-100%);
        animation: shine 2s infinite;
      }

      .address-section {
        padding: 2rem;
        border-radius: 16px;
        margin-bottom: 1.5rem;
        background: rgba(255, 255, 255, 0.8);
        border: 1px solid rgba(255, 255, 255, 0.2);
        animation: slideIn 0.5s ease-out 0.3s both;
      }

      .address-section h4 {
        font-size: 1.25rem;
        font-weight: 600;
        color: var(--text);
        margin-bottom: 1.5rem;
        padding-bottom: 1rem;
        border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        display: flex;
        align-items: center;
        gap: 0.75rem;
      }

      .address-section h4 i {
        font-size: 1.25rem;
        color: var(--text-light);
      }

      .address-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 2rem;
      }

      .address-item {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
      }

      .address-label {
        font-size: 0.875rem;
        color: var(--text-light);
        text-transform: uppercase;
        letter-spacing: 0.05em;
      }

      .address-value {
        font-size: 1rem;
        color: var(--text);
        line-height: 1.5;
      }

      .amount-section {
        margin-top: 1.5rem;
        padding: 2rem;
        border-radius: 16px;
        background: rgba(255, 255, 255, 0.8);
        border: 1px solid rgba(255, 255, 255, 0.2);
        animation: slideIn 0.5s ease-out 0.4s both;
      }

      .amount-section h4 {
        font-size: 1.25rem;
        font-weight: 600;
        color: var(--text);
        margin-bottom: 1.5rem;
        padding-bottom: 1rem;
        border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        display: flex;
        align-items: center;
        gap: 0.75rem;
      }

      .amount-section h4 i {
        font-size: 1.25rem;
        color: var(--text-light);
      }

      .amount-grid {
        display: grid;
        gap: 1rem;
      }

      .amount-row {
        display: grid;
        grid-template-columns: 1fr auto;
        align-items: center;
        padding: 1rem;
        border-radius: 8px;
        background: rgba(255, 255, 255, 0.5);
        transition: all 0.2s ease;
      }

      .amount-row:hover {
        background: rgba(255, 255, 255, 0.8);
      }

      .amount-row:last-child {
        background: var(--text);
        color: white;
        margin-top: 0.5rem;
      }

      .amount-label {
        font-size: 0.95rem;
        color: var(--text);
      }

      .amount-row:last-child .amount-label {
        color: rgba(255, 255, 255, 0.9);
      }

      .amount-value {
        font-weight: 600;
        font-size: 1rem;
      }

      .amount-row:last-child .amount-value {
        color: white;
        font-size: 1.1rem;
      }

      .payment-pending .amount-row:last-child {
        background: linear-gradient(135deg, #7928CA, #FF0080);
      }

      .pending .amount-row:last-child {
        background: linear-gradient(135deg, #3B82F6, #8B5CF6);
      }

      .success .amount-row:last-child {
        background: linear-gradient(135deg, #0EA5E9, #10B981);
      }

      .cancelled .amount-row:last-child {
        background: linear-gradient(135deg, #EF4444, #F43F5E);
      }

      @media (max-width: 768px) {
        .page-container {
          padding: 1rem;
        }

        .order-content {
          grid-template-columns: 1fr;
          padding: 1.5rem;
        }

        .success-header {
          padding: 2rem 1.5rem;
        }
      }
    </style>
  </head>
  <body class="<%= order.status === 'Payment Pending' ? 'payment-pending' : 
                  (order.status === 'Pending' ? 'pending' : 
                   (order.status === 'Cancelled' ? 'cancelled' : 'success')) %>">
    <div class="background-pattern"></div>
    <div class="page-container <%= 
      order.status === 'Payment Pending' ? 'payment-pending' :
      order.status === 'Pending' ? 'pending' :
      order.status === 'Delivered' ? 'delivered' :
      order.status === 'Cancelled' ? 'cancelled' : 'success'
    %>">
      <div class="order-success-container">
        <div class="success-header <%= 
          order.status === 'Payment Pending' ? 'payment-pending' :
          order.status === 'Pending' ? 'pending' :
          order.status === 'Delivered' ? 'delivered' :
          order.status === 'Cancelled' ? 'cancelled' : 'success'
        %>">
          <div class="success-icon <%= order.status === 'Payment Pending' ? 'payment-pending' : (order.status === 'Pending' ? 'pending' : 'success') %>">
            <i class="fas <%= 
              order.status === 'Payment Pending' ? 'fa-clock' : 
              (order.status === 'Pending' ? 'fa-spinner fa-spin' : 'fa-check') 
            %>"></i>
          </div>
          <div class="status-badge <%= order.status === 'Payment Pending' ? 'payment-pending' : (order.status === 'Pending' ? 'pending' : 'success') %>">
            <%= order.status %>
          </div>
          <h2>
            <% if (order.status === "Payment Pending") { %>
              Order Created - Payment Pending
              <div class="mt-4">
                <a href="/payment/<%= order.orderId %>" class="action-button payment-pending">
                  <i class="fas fa-credit-card me-2"></i>Complete Payment
                </a>
              </div>
            <% } else { %>
              Order Placed Successfully!
            <% } %>
          </h2>
          <p class="text-muted mt-3">
            <% if (order.status === "Payment Pending") { %>
              Please complete your payment to confirm the order.
            <% } else { %>
              Thank you for shopping with us.
            <% } %>
          </p>
        </div>

        <div class="order-content">
          <% if (!order) { %>
            <div class="error-message">
              <h2>Order Not Found</h2>
              <p>Sorry, we couldn't find the order you're looking for.</p>
              <a href="/orders" class="btn btn-primary">View All Orders</a>
            </div>
          <% } else { %>
            <div class="address-section">
              <h4><i class="fas fa-map-marker-alt"></i> Delivery Address</h4>
              <% if (order.formattedAddress) { %>
                <div class="address-grid">
                  <div class="address-item">
                    <span class="address-label">Full Name</span>
                    <span class="address-value"><%= order.formattedAddress.name %></span>
                  </div>
                  <div class="address-item">
                    <span class="address-label">Phone Number</span>
                    <span class="address-value"><%= order.formattedAddress.phone %></span>
                  </div>
                  <div class="address-item">
                    <span class="address-label">House/Building</span>
                    <span class="address-value"><%= order.formattedAddress.houseName %></span>
                  </div>
                  <div class="address-item">
                    <span class="address-label">Street Address</span>
                    <span class="address-value"><%= order.formattedAddress.address %></span>
                  </div>
                  <div class="address-item">
                    <span class="address-label">City</span>
                    <span class="address-value"><%= order.formattedAddress.city %></span>
                  </div>
                  <div class="address-item">
                    <span class="address-label">State</span>
                    <span class="address-value"><%= order.formattedAddress.state %></span>
                  </div>
                  <div class="address-item">
                    <span class="address-label">PIN Code</span>
                    <span class="address-value"><%= order.formattedAddress.pincode %></span>
                  </div>
                  <% if (order.formattedAddress.altPhone) { %>
                  <div class="address-item">
                    <span class="address-label">Alternate Phone</span>
                    <span class="address-value"><%= order.formattedAddress.altPhone %></span>
                  </div>
                  <% } %>
                </div>
              <% } else { %>
                <p class="text-muted">No address information available</p>
              <% } %>
            </div>

            <div class="amount-section">
              <h4><i class="fas fa-receipt"></i> Order Summary</h4>
              <div class="amount-grid">
                <div class="amount-row">
                  <span class="amount-label">Subtotal</span>
                  <span class="amount-value">₹<%= order.totalPrice ? (order.totalPrice - (order.tax || 0)).toFixed(2) : '0.00' %></span>
                </div>
                <% if (order.discount) { %>
                <div class="amount-row">
                  <span class="amount-label">Discount</span>
                  <span class="amount-value">- ₹<%= order.discount.toFixed(2) %></span>
                </div>
                <% } %>
                <div class="amount-row">
                  <span class="amount-label">Tax</span>
                  <span class="amount-value">₹<%= order.tax ? order.tax.toFixed(2) : '0.00' %></span>
                </div>
                <% if (order.shippingCost) { %>
                <div class="amount-row">
                  <span class="amount-label">Shipping</span>
                  <span class="amount-value">₹<%= order.shippingCost.toFixed(2) %></span>
                </div>
                <% } %>
                <div class="amount-row">
                  <span class="amount-label">Total Amount</span>
                  <span class="amount-value">₹<%= order.totalPrice ? order.totalPrice.toFixed(2) : '0.00' %></span>
                </div>
              </div>
            </div>

            <div class="ordered-items">
              <h4 class="mb-4">Order Details</h4>
              <% if (order.formattedItems && order.formattedItems.length > 0) { %>
                <% order.formattedItems.forEach((item, index) => { %>
                  <div class="item-card" style="--item-index: <%= index %>">
                    <h6 class="mb-2"><%= item.name %></h6>
                    <div class="text-muted small mb-2">
                      <%= item.specifications.RAM %> RAM | 
                      <%= item.specifications.Storage %> Storage | 
                      <%= item.specifications.color %>
                    </div>
                    <div class="d-flex justify-content-between align-items-center">
                      <span class="text-muted">Qty: <%= item.quantity %></span>
                      <span class="fw-bold">₹<%= item.totalPrice.toFixed(2) %></span>
                    </div>
                  </div>
                <% }) %>
              <% } else { %>
                <p class="text-muted">No items found in this order</p>
              <% } %>
            </div>

            <div class="delivery-time">
              <i class="fas fa-truck me-2"></i>
              Expected Delivery: Within 3-5 Business Days
            </div>

            <a href="/orders" class="btn btn-primary btn-lg btn-view-orders">
              View My Orders
            </a>
          <% } %>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
